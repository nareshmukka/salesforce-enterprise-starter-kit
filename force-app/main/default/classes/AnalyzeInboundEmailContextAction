public with sharing class AnalyzeInboundEmailContextAction {
    public class Request {
        @InvocableVariable(required=true)
        public String requestJson;
    }

    public class Response {
        @InvocableVariable
        public String responseJson;
    }

    @InvocableMethod(
        label='Analyze Inbound Email Context'
        description='Analyzes inbound email context and returns summary, category, intent, confirmation number, new email address, and confidence as JSON.'
    )
    public static List<Response> analyze(List<Request> requests) {
        List<Response> responses = new List<Response>();

        for (Request req : requests) {
            Response res = new Response();

            // Placeholder logic for now.
            // Later replace with your real prompt/action logic if needed.
            Map<String, Object> result = new Map<String, Object>{
                'summary' => 'Customer is asking to resend confirmation email.',
                'category' => 'Resend Request',
                'intent' => 'RESEND',
                'confirmationNumber' => 'ABC12345',
                'newEmailAddress' => 'newmail@example.com',
                'confidence' => 0.92
            };

            res.responseJson = JSON.serialize(result);
            responses.add(res);
        }

        return responses;
    }
}
